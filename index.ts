/**
 * Package entry point for the `vstring` library - virtual (zero-copy) strings
 * for AssemblyScript. `VString` is a PascalCase alias of `vstring`; both refer
 * to the same class.
 */
export { vstring, vstring as VString } from "./assembly/index";
